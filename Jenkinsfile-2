pipeline {
  agent any
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook -i inventory/my-cluster/hosts.ini site.yml'
      }
    }
  }
}
